# STRATA 学生学习分析与分层教学开发计划

> 更新日期：2026-07-19  
> 用途：指导日常开发、测试和验收。论文研究方法与统计细节见[学生行为分析与隐性分层设计报告](student_behavior_ai_stratification_design.md)。

## 1. 当前做到哪里

| 阶段 | 状态 | 当前结果 |
| --- | --- | --- |
| 基础安全与权限 | 已完成 | 学生端不显示内部层级、模型判断和分组依据；教师只能查看任教范围；文件和 WebSocket 已增加权限检查 |
| 学习记录底座 | 已完成 | 课堂、题目、资源、签到、互动、评价和积分均可写入统一学习记录，并保留新旧记录核对能力 |
| 学习数据检查 | 已完成 | 学校管理员可查看本校检查结果、导出 XLSX，并由夜间任务自动运行 |
| 模拟数据 | 已完成 | 可创建独立模拟学校或在当前学校生成带标记的测试班级、学生和学习记录，也可整批清理 |
| 评价与题目准备 | 进行中 | 评价闭环和题目从草稿、审核、试用、启用到停用的状态管理已完成；下一步开发共同测试题和不同版本结果比较 |
| 学生学习情况汇总 | 未开始 | 尚未建立正式的学生特征定义、时间窗口汇总和可重复计算任务 |
| 教师分层建议 | 未开始 | 尚未建立教师审核页面和学生不可见的分层建议流程 |
| 模型试验与班级调整 | 未开始 | 尚未训练正式模型，也没有可以上线的班级专属模型 |
| 学校试用与跨校分析 | 未开始 | 尚未形成真实学校试用结果和跨校验证结论 |

当前准确位置是：**数据底座、评价闭环和题目状态管理已完成，正在开发共同测试与版本比较，尚未进入正式特征和模型开发。**

## 2. 固定开发顺序

```text
评价标准进入课时与课堂（已完成）
  -> 评价缺少材料的处理与覆盖数（已完成）
  -> 题目从创建到停用的完整管理（已完成）
  -> 共同测试和不同版本结果比较
  -> 学生学习情况按日、7 日、30 日和单元汇总
  -> 教师可查看、学生不可见的规则建议
  -> 机器学习模型比较
  -> 班级调整参数和夜间更新
  -> 校内正式试用
  -> 跨校独立验证和统一分析
```

不能跳过评价、题目和学习情况汇总，直接用开发库训练正式模型。

## 3. 当前开发任务

### 3.1 评价试用与审核记录

状态：**已完成，并已继续完成课堂使用闭环。** 当前小榄中学已生成 4 条带“测试-”前缀的记录用于页面和导出验证。

教师需要能够：

- 选择一个已发布的评价标准版本。
- 记录内容审核、课堂试用、评分培训和评分一致性检查。
- 保存日期、参与人数、完成状态、处理结论、发现的问题和修改说明。
- 对评分一致性检查填写一致率。
- 查看历史记录并导出 XLSX。
- 使用模拟数据先验证完整页面和接口流程。

完成条件：

- 数据库迁移、教师课程权限、API、Vue 页面和 XLSX 导出全部完成。
- 其他教师和学校管理员不能读取或修改当前教师记录。
- 未发布的评价标准不能创建试用记录。
- 已完成记录保留历史，避免误删。
- 自动化测试、Vue 构建和桌面/手机页面检查通过。

课堂使用规则：

- 评价标准由教师在标准库维护，课时环节只选择已发布版本。
- 课堂首次开启评价时冻结版本，后续工作稿修改不影响历史课堂。
- 评价提交关联同一学生在本课堂、本环节的作答和作品，缺少材料时不自动给低星。
- 每个指标选择 1-5 星或暂不评价；平均星级排除暂不评价项，同时保留原因和覆盖数。
- 正式评价版本写入学习事件，旧课程评价兼容保留但不自动进入正式标准证据。

### 3.2 题目完整管理

状态：**已完成。** 题库题目已按以下状态运行：

```text
个人可用 -> 申请共享 -> 待审核 -> 可试用 -> 已启用 -> 已停用
```

同时记录：

- 人工创建或 AI 生成。
- 创建教师和所属学校。
- 审核人、审核意见和审核时间。
- 试用次数、作答人数、正确率和选项分布。
- 是否可以进入正式测试和共同测试。
- 发现答案错误、泄题或明显异常后停用。

已完成规则：

- 教师创建、XLSX 导入和 AI 确认均先保存到“我的题目”，本人无需审核即可组卷。
- 只有教师主动申请校内共享时，题目才进入学校管理员审核列表。
- 教师申请共享后，学校管理员可退回或准许试用。
- 可试用题只允许创建教师组卷；至少有 1 次真实学生作答后才可正式启用。
- 已启用题进入本校共享题库，其他教师只读并可组卷。
- 题目内容保存不可修改版本；试卷记录实际引用版本和当时状态。
- 使用次数、作答人数、正确率和选项分布从试卷与答卷计算，不维护人工递增计数。
- 教师和学校管理员页面、XLSX 导出、API 权限和自动化测试已完成。
- 个人/共享范围和审核状态是题库权限字段，不进入学生分层特征或标签；学生端不接收这些内部字段。

### 3.3 共同测试和版本比较

- 每个学科保留一部分各班都完成的共同题目。
- A、B、C 或 A/B、B/C 专属题不能替代共同题目。
- 保存题目版本，旧答卷继续对应旧版本。
- 比较不同学期、不同课程版本和不同学校前，先确认题目仍可比较。
- 评分差异过大时只提示重新检查，不直接用于分层。

### 3.4 学生学习情况汇总

前述工作完成后再开发：

- 学生日、7 日、30 日和单元学习情况。
- 完成率、按时提交、首次作答、订正、资源学习和课堂参与。
- 没有任务、请假、离线和数据错误分别记录，不能统一算成 0。
- 汇总结果可按固定日期重新计算，不能使用未来数据。

### 3.5 教师分层建议

- 教师看到当前学习情况、建议任务难度、原因和需要补充的数据。
- 教师可以接受、保持、调整或延后。
- 学生只看到实际获得的任务和帮助，不显示 A/B/C、概率和排名。
- 学校管理员只看汇总，不默认查看学生个人内部层级。

### 3.6 模型试验

先比较透明规则、统计模型、CatBoost 和 LightGBM，不预先指定唯一模型。

- 所有模型使用相同训练数据和测试范围。
- 先检查是否比简单规则更可靠。
- 数据不足时不生成建议。
- 每个班保存自己的调整参数，不用几十名学生从零训练复杂模型。
- 新模型没有通过检查时继续使用旧版本。

## 4. 测试数据怎么使用

当前没有足够真实数据不影响工程开发。模拟数据可以用于：

- 验证数据库、接口、页面、导出和夜间任务。
- 验证同一批数据重复运行不会产生重复记录。
- 验证异常数据、缺失数据和任务失败后的处理。
- 提前开发教师页面和统计图表。

模拟数据不能用于：

- 宣称模型对真实学生有效。
- 宣称动态分层改善了学习效果。
- 替代真实教师审核、正式课堂试用和评分一致性检查。
- 与正式学校数据混合生成对外报告。

## 5. 技术边界

- 正式部署使用 PostgreSQL、Redis、Celery 和 Django Channels。
- 当前本机可使用 SQLite 快速开发，但涉及并发、锁和夜间任务时必须补做 PostgreSQL 验证。
- 学生个人数据和原始答卷不得发送给教师自带的外部 AI 接口。
- 教师自带 DeepSeek API 只用于生成可修改的个人题目，不能直接进入校内共享题库或自动改变学生分层。
- 每次数据库调整都要有迁移、测试、回滚说明和数据清理方法。
- 页面调整后检查桌面和手机视口，临时截图检查后删除。

## 6. 每阶段验收

每个阶段只有同时满足以下条件才算完成：

- 后端模型、权限和接口完成。
- 前端页面可以完成真实操作，不是静态演示。
- SQLite 和 PostgreSQL 关键测试通过。
- Vue 类型检查和生产构建通过。
- 文档、API 说明和数据模型同步更新。
- Git 提交可单独回滚。

## 7. 当前测试基线

- Django：SQLite 与 PostgreSQL 各 114 项测试通过。
- Vue：类型检查与生产构建通过。
- PostgreSQL、Redis、Channels 和 Celery 已完成本机组件验证。
- 当前开发服务：`http://127.0.0.1:8010`。
- 当前正式学校记录量不足，模型开发继续使用隔离的模拟数据。
